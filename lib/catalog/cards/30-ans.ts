import type { Card } from "../../catalog";

// 30e Anniversaire - genere par scripts/generate-30ans.mjs
// Lance `npm run generate:30ans` pour telecharger les visuels FR et remplir ce
// fichier. Tant que le script n'a pas tourne, la serie s'affiche vide.
export const TRENTE_ANS_CARDS = ([] as const) as readonly Card[];
